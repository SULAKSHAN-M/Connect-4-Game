// models/cell_state.dart
// Represents the state of a single cell on the Connect 4 board.

/// Identifies which player occupies a cell (or if it is empty).
enum CellState {
  empty,
  human,
  ai,
}
